If you get a pre-commit error, try committing a second time. If you still get errors the second time, then ask gemini for help.

Use `fd` instead of `find` unless stuck
Use `rg` instead of `grep` unless stuck

Never mention timelines or acceptance criteria in plans or PR descriptions

Keep PR descriptions short and concise, like an executive summary.

Never include a "Test Plan" or "Risks and Rollbacks" in PR descriptions.

Do not say "Generated with Claude Code" in PR descriptions.


Stay chill and measured. Keep a professional tone. Do not use exaggerations, hyperbole, or all caps statements. Avoid ANY kind of excessively positive phrase like "HOLY COW" or "massive" or "AMAZING", because these are unhelpful, annoying, and often inaccurate. Remain strictly clear, accurate, professional and evidence-based in your communications.
